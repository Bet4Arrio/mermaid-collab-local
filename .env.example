PORT=3000
DB_URL=postgres://postgres:postgres@localhost:5432/mermaid_collab?sslmode=disable
REDIS_URL=redis://localhost:6379
ENV=development

# Extra allowed WebSocket origins (comma-separated). Same-origin is always
# allowed; in development the Vite dev server (http://localhost:5173) is too.
# Example for production: ALLOWED_ORIGINS=https://app.example.com
ALLOWED_ORIGINS=
